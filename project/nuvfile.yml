version: 3

tasks:
  deploy:
    silent: true
    cmds:
      - |
        export DEPLOY_PATH=$(realpath ./)
        if [ ! -z {{._path_}} ]; then
          DEPLOY_PATH=$(realpath {{._path_}})
        fi
        nuv -wsk project deploy --project $DEPLOY_PATH/packages
